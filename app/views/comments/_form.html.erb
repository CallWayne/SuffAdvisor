<%= form_for [commentable, Comment.new] do |f| %>
    <div class="well">
      <%= f.text_area :body, class: "form_contol", placeholder: "Add a comment" %>
    </div>
    <%= f.submit%>
<% end %>
<br>